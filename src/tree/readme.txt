BinarySearchTree with recursive and non-recursive algorithms.

TestClass is class with main which addd 14 nodes to tree and then delete some or even all of them to test execution of both algorithms.
Change boolean parameter in the begining of the file to switch between modes (resursive or not).
